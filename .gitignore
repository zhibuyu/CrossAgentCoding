# 打包产物输出目录（不提交，打 tag 时作为发行版附件）
/release/

# 构建中间文件（IExpress 打包过程）
/_manager_stage/
/_manager_build.sed

# AgentMemory 服务在启动目录生成的运行时数据
/data/

# Stray Windows PowerShell module cache
/Microsoft/

# 本地密钥与配置（含 API Key）——只保留在本机数据目录，绝不提交到 git
settings.json
**/settings.json
.env
.env.*
*.local.json
*.secret
secrets.json
*.key
